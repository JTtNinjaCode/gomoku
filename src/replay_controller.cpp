#include "replay_controller.h"

bool ReplayController::Load(const std::string& path) { return false; }
void ReplayController::Run(const Grid& grid) const {}
Board ReplayController::BoardAtStep(int step) const { return Board(); }
